if(DEFINED CONFIG_SAMPLE_SUPPORT_SLE_AB)
    add_subdirectory_if_exist(sle_ab)
endif()

if(DEFINED CONFIG_SAMPLE_SUPPORT_SLE_CARD)
    add_subdirectory_if_exist(sle_card)
endif()

set(SOURCES "${SOURCES}" PARENT_SCOPE)
set(PUBLIC_HEADER "${PUBLIC_HEADER}" PARENT_SCOPE)
set(LIBS "${LIBS}" PARENT_SCOPE)
