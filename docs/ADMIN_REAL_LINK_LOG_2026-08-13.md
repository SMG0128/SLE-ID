# 管理端真实通讯链路接入日志（2026-08-13）

## 已完成

- 管理端事件、报警、设备、许可、卡片、邀请码、系统设置接口全部切换为真实 REST。
- Axios 自动携带本地保存的共享访问令牌，并显示后端返回的业务错误。
- `/ws/events` 已启用，具备断线指数退避重连、连接状态通知和事件订阅。
- 新增“待确认”页面：展示权威待确认列表、超时倒计时、允许/拒绝操作和实时刷新。
- 首页硬编码统计、趋势和异常饼图已替换为后端事件、报警、设备和串口真实数据。
- 去除许可编辑、设备策略更新、数据库导出等后端尚不支持但会提示成功的假操作。
- 许可创建与硬件策略下发拆分；只有 B 返回成功后才提示下发完成。
- 系统设置可保存访问令牌并重连 WebSocket；数据库备份调用真实后端。

## 同步位置与备份

- 管理端仓库：`D:\naiwa1\SLE-ID-Admin-Backend\SLE-ID-Admin-Backend`
- 前端工作副本：`admin-frontend/`
- 原前端备份：`backups/admin-frontend-before-real-api-2026-08-13/`
- 原后端备份：`backups/admin-server-before-security-sync-2026-08-13/`

## 验证结果

- 前端 `vue-tsc --noEmit && vite build`：通过。
- 后端 `npm run check`：编译通过，13/13 测试通过。
- 后端临时监听 `127.0.0.1:18080`：健康接口、待确认接口和前端托管均返回 200/正常响应。
- A/B/Card 全部主机协议、安全、互操作、单 USB 网关及写卡 DryRun 回归通过。

## 已知边界

- 没有第三块板，本轮未验证实体 Card SLE 链路。
- 未连接真实 B 串口时，确认页只能验证接口与空列表，不能验证 GPIO 实际动作。
- 首页趋势最多聚合后端单次查询返回的 200 条当日事件；高流量场景后续应增加后端聚合接口。
- Element Plus 与 ECharts 仍产生较大的生产块，这是性能优化项，不影响功能和部署。
